# Feature: project deletion and role management

1. Admins need to be able to delete projects. Add a `deleteProject(actor, projectId)` operation and show a Delete button on the project page for admins.
2. From the members page, admins should be able to change a member's role. Add a `changeRole(actor, memberUserId, newRole)` operation and a role selector in the members UI.

Add tests.
