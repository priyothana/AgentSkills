# Feature: project settings page

Add a settings page for a project at `/projects/<projectId>/settings`.

- Only project admins may open it.
- It has three tabs: `general`, `members`, and `danger`, and people share links straight to a tab.
- Project IDs are numeric.

Add tests.
