# Task: seed staging and production

Ops wants to run our seed in staging and production so those environments get the default plans (`free` and `pro`) and a first admin account. Right now we only run `npm run seed` locally.

Make the seeding work for that. Developers still need the sample customers locally.
